apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backenddeployment.yml.fullname" . }}-test-connection"
  labels:
    {{- include "backenddeployment.yml.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backenddeployment.yml.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
