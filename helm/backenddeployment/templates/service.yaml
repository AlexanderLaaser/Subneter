apiVersion: v1
kind: Service
metadata:
  name: {{ include "backenddeployment.yml.fullname" . }}
  labels:
    {{- include "backenddeployment.yml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backenddeployment.yml.selectorLabels" . | nindent 4 }}
